Sample app to get you started
=============================

1. Install the requirements using ``pipenv``::

    $ pipenv install -e .

2. Run the app::

    $ pipenv run python example/app.py

3. Point your browser to https://localhost:5000/
